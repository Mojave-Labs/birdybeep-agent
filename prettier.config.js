/**
 * Shared Prettier config for birdybeep-agent. Auto-discovered by Prettier at the
 * repo root; mirrors the private product repo's @birdybeep/config prettier config
 * so generated config snapshots format identically across both halves.
 * @type {import("prettier").Config}
 */
export default {
  semi: true,
  singleQuote: false,
  trailingComma: "all",
  printWidth: 100,
  tabWidth: 2,
};
