# Example — OpenCode

> **Stub.** Generated, redacted example config for the OpenCode adapter lands in the
> `a-docs` epic (`examples/{claude-code,codex,opencode}` ticket). It will show the
> global plugin registration BirdyBeep adds, and the restart-to-load flow.
