# Example — Codex

> **Stub.** Generated, redacted example config for the Codex adapter lands in the
> `a-docs` epic (`examples/{claude-code,codex,opencode}` ticket). It will show the
> `notify` command + lifecycle hooks BirdyBeep adds, and the one-time trust flow.
