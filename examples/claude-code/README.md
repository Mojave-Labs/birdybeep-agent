# Example — Claude Code

> **Stub.** Generated, redacted example config for the Claude Code adapter lands in the
> `a-docs` epic (`examples/{claude-code,codex,opencode}` ticket). It will show exactly
> what BirdyBeep adds to `~/.claude/settings.json` and what an emitted event looks like.
