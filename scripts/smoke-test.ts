/**
 * scripts/smoke-test.ts — post-build smoke test of the published-shape CLI.
 *
 * Stub for the A-MONOREPO scaffold so §16.3's `scripts/` has its expected home.
 * The real implementation (install the built CLI into a temp prefix, run
 * `birdybeep doctor`, assert it works) lands in the `a-release` epic
 * (`scripts/smoke-test.ts` ticket).
 */

console.error("scripts/smoke-test.ts is not implemented yet — see the a-release epic (bd ready).");
process.exit(1);
