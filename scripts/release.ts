/**
 * scripts/release.ts — release orchestration for @birdybeep/* packages.
 *
 * Stub for the A-MONOREPO scaffold so §16.3's `scripts/` has its expected home.
 * The real implementation (build → version/changeset → publish handoff) lands in
 * the `a-release` epic (`scripts/release.ts` ticket). The first real publish is a
 * HUMAN-REQUIRED gate (npm org + tokens).
 */

console.error("scripts/release.ts is not implemented yet — see the a-release epic (bd ready).");
process.exit(1);
