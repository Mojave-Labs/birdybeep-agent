node_modules/
dist/
.turbo/
coverage/
pnpm-lock.yaml

# Vitest snapshots — owned by vitest's own formatter, not Prettier.
**/__snapshots__/

# Tool-managed config — owned by its tool, not the formatter
.claude/
.beads/
.dolt/

# Human-owned governance docs (kept in sync with the private product repo) —
# not reformatted here to avoid cross-repo drift.
AGENTS.md
BUILD-PLAN.md
CLAUDE.md
docs/SPEC.md

# Generated-config examples — verbatim installer output, kept byte-for-byte in
# lockstep with the adapters' __snapshots__. Owned by the installer, not the formatter.
examples/claude-code/settings.json
examples/codex/config.toml
examples/opencode/opencode.json
