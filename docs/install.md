# Install

> **Stub.** Full install guide lands in the `a-docs` epic (`docs/install.md` ticket).

Install the CLI globally and pair your machine:

```bash
npm install -g @birdybeep/cli
birdybeep login
birdybeep agent install
```
