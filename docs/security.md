# Security

> **Stub.** Full security notes land in the `a-docs` epic (`docs/security.md` ticket).

What data leaves your machine (and what does not), how tokens are stored (OS keychain
with a strict-perm file fallback), payload redaction/truncation, path hashing, and the
best-effort local queue.
