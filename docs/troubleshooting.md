# Troubleshooting

> **Stub.** Full troubleshooting guide lands in the `a-docs` epic (`docs/troubleshooting.md` ticket).

Common failure modes and `birdybeep doctor` diagnostics: missing token, untrusted Codex
hooks (`needs_trust`), OpenCode needing a restart (`needs_restart`), and the offline queue.
