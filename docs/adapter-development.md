# Adapter development

> **Stub.** Full adapter authoring guide lands in the `a-docs` epic (`docs/adapter-development.md` ticket).

How to implement the `AgentAdapter` interface (`detect` / `install` / `uninstall` /
`status` / `doctor` / `normalizeEvent`) for a new coding harness, with the snapshot- and
E2E-testing bar every adapter must meet.
